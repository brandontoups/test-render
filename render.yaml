services:
  - type: cron
    name: test
    env: python
    schedule: "0/5 * * * *"
    buildCommand: "true" # ensure it's a string
    startCommand: python test.py
    buildFilter:
      paths:
      - test/**
    rootDir: test
